import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.apmi.asistente',
  appName: 'asistente',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
